@extends('layouts.admin')@section('title','Dashboard')@section('content')<div class="admin-page-head"><div><p class="admin-eyebrow">OVERVIEW</p><h1>Dashboard</h1></div><a class="admin-button" href="{{ route('admin.posts.create') }}">+ Bài viết mới</a></div><div class="metric-admin-grid">@foreach($metrics as $key=>$value)<article><span>{{ ucfirst($key) }}</span><strong>{{ number_format($value) }}</strong></article>@endforeach</div><section class="admin-panel"><div class="admin-panel-head"><h2>Hoạt động gần đây</h2><a href="{{ route('admin.activity.index') }}">Xem tất cả</a></div><div class="admin-table-wrap"><table><thead><tr><th>Thời gian</th><th>Người dùng</th><th>Sự kiện</th><th>Đối tượng</th></tr></thead><tbody>@forelse($activities as $activity)<tr><td>{{ $activity->created_at?->format('d/m/Y H:i') }}</td><td>{{ $activity->user?->name ?? 'System' }}</td><td><span class="status-pill">{{ $activity->event }}</span></td><td>{{ class_basename($activity->auditable_type ?? '') }} #{{ $activity->auditable_id }}</td></tr>@empty<tr><td colspan="4">Chưa có hoạt động.</td></tr>@endforelse</tbody></table></div></section>@endsection
