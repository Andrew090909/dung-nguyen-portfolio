@extends('layouts.admin')@section('title','Chi tiết liên hệ')@section('content')<div class="admin-page-head"><div><p class="admin-eyebrow">COMMERCIAL ASSESSMENT</p><h1>{{ $contact->name }}</h1></div><a class="admin-button secondary" href="{{ route('admin.contacts.index') }}">← Quay lại</a></div><section class="admin-panel contact-detail"><dl><div><dt>Doanh nghiệp</dt><dd>{{ $contact->company }}</dd></div><div><dt>Email</dt><dd><a href="mailto:{{ $contact->email }}">{{ $contact->email }}</a></dd></div><div><dt>Điện thoại</dt><dd>{{ $contact->phone }}</dd></div><div><dt>Nhu cầu</dt><dd>{{ $contact->need }}</dd></div><div><dt>Ngân sách</dt><dd>{{ $contact->budget }}</dd></div><div><dt>Timeline</dt><dd>{{ $contact->timeline }}</dd></div><div><dt>Locale</dt><dd>{{ $contact->locale }}</dd></div></dl><h2>Bối cảnh</h2><p class="message-box">{{ $contact->message }}</p>@if(auth()->user()->hasRole('super_admin'))<form method="post" action="{{ route('admin.contacts.destroy',$contact) }}" onsubmit="return confirm('Xóa liên hệ này?')">@csrf @method('DELETE')<button class="danger-button" type="submit">Xóa yêu cầu</button></form>@endif</section>@endsection
