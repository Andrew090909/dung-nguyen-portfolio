<div class="translation-grid">@foreach(['vi'=>'VI','en'=>'EN','zh'=>'中文'] as $locale=>$label)<label><span>{{ $label }} — {{ $labelText }}</span>@if(($type ?? 'input')==='textarea')<textarea name="{{ $name }}[{{ $locale }}]" rows="{{ $rows ?? 4 }}" {{ ($required ?? false)?'required':'' }}>{{ old($name.'.'.$locale, $item->{$name}[$locale] ?? '') }}</textarea>@else<input name="{{ $name }}[{{ $locale }}]" value="{{ old($name.'.'.$locale, $item->{$name}[$locale] ?? '') }}" {{ ($required ?? false)?'required':'' }}>@endif</label>@endforeach</div>
